{
  "name": "garden-defenders",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Top-down garden survival game. Defend your plots from waves of insects.",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
